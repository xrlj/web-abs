import { TextColorThemeDirective } from './text-color-theme.directive';

describe('TextColorThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new TextColorThemeDirective();
    expect(directive).toBeTruthy();
  });
});
